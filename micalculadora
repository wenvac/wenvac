import java.util.Scanner;
public class Micalculadora implements Operaciones {

    Scanner leer = new Scanner (System.in);
    int operaciones = 0;
      System.out.print("digite la cantidad de numeros que desea operar:");
    int numero= leer.nextInt();
    for (int i = 1; i < numero; i++){   
      System.out.print("ingrese el" + i + "numero: ");
      
    }

    @Override
    public int suma(int numero) {
        
        
    }

    @Override
    public int resta(int numero) {
    }

    @Override
    public int division(int numero) {
        
    }
    @Override
    public int multiplicacion(int numero) {
        
    }
    
    
}
[7:22 a. m., 27/5/2024] Daniel: package com.mycompany.calculadora;
import java.util.Scanner;

public class Micalculadora implements Operaciones {
    
    Scanner leer = new Scanner (System.in);
    
public void realizarOperaciones (){
    System.out.print("digite la cantidad de numeros que desea operar:");
    
    int cantidad = leer.nextInt();
    int resultado =0;
    
        int numero = leer.nextInt();
        resultado += numero; 
        
    
      System.out.print("el resultado de la operacion es:");
    }

    @Override

    public int suma(int numero) {
int resultado = 0;

         for (int i = 0; i < numero; i++){ 
             
             System.out.println("Ingrese el numero" +(i+1));
             int numero1=leer.nextInt();
             resultado+=numero1;
         }
    return resultado;
        
    }

    @Override

    public int resta(int numero) {
int resultado = 0;
         for (int i = 1; i < numero; i++){ 
                resultado-=numero;
         }
return resultado;
    }

    @Override

    public int division(int numero) {
int resultado = 0;
         for (int i = 1; i < numero; i++){ 
                resultado/=numero;
         }
        return resultado;
    }
    @Override

    public int multiplicacion(int numero) {
int resultado = 0;
         for (int i = 1; i < numero; i++){ 
                resultado*=numero;
         }
            return resultado;
     }
